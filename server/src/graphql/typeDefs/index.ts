import { userTypes } from "./auth";
import { addressTypes } from "./address";
import { categoryTypes } from "./category";
import { productTypes } from "./product";

export const typeDefs = [userTypes, addressTypes, categoryTypes, productTypes];
